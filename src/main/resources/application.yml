server:
  port: 9090
cloud:
  aws:
    credentials:
      access-key:
      secret-key:
    region:
      static: ap-south-1
    stack:
      auto: false

application:
  bucket:
    name: venkatbucket0107